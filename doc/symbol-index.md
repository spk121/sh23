# Symbol Index

In this section, we give a list of symbolic constructs that may occur in shell scripts and point to the documentation sections that may provide context for those constructs.

| Symbol | Context |
|--------|---------|
| `( ... )` | subshell, arithmetic grouping |
| `{ ... }` | grouping |
| `$( ...)` | |
| `$(( ... ))` | |
| 
